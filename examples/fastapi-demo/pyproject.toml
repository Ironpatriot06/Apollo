[project]
name = "fastapi-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11.8"
dependencies = [
    "aiosqlite>=0.22.1",
    "apollo-sdk",
    "fastapi>=0.141.1",
    "sqlalchemy>=2.0.52",
    "uvicorn>=0.52.1",
]

[tool.uv.sources]
apollo-sdk = { path = "../../sdk", editable = true }
